services:
  gst-scraper:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4192:4192"
    environment:
      - KRUTRIM_API_KEY=${KRUTRIM_API_KEY:-}
      - PORT=${PORT:-4192}
    restart: unless-stopped
