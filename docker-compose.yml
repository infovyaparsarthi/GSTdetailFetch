services:
  gst-scraper:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "4192"
    ports:
      - "4192"
    environment:
      - KRUTRIM_API_KEY=${KRUTRIM_API_KEY:-}
      - PORT=${PORT:-4192}
    restart: unless-stopped
